<Page x:Class="Client.Views.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Client.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      xmlns:prism="http://prismlibrary.com/" 
      xmlns:viewmodel="clr-namespace:Client.ViewModels" 
      prism:ViewModelLocator.AutoWireViewModel="True"
      >

    <Grid>
        <Label Content="Main page " HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,20,0,0" FontSize="18"/>

        <Button Content="Users list" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,60,0,0" Width="100" Command="{Binding NavigateToUsersPageCommand}"/>

        <Button Content="Sign in" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,100,0,0" Width="100" Command="{Binding NavigateToSignPageCommand}"/>

        <Button Content="Registration" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,140,0,0" Width="100" Command="{Binding NavigateToRegistrationPageCommand}"/>
    </Grid>
</Page>
